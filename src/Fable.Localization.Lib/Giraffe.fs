module Fable.Localization.Giraffe

//open System.Resources

//let localizationsToGlobalVarHandler(jsVarName:string, resourceManager : ResourceManager, allKeys : string array, allLanguages : string array, defaultLanguage : string) : HttpHandler =
    
//    if not (allKeys |> Seq.contains defaultLanguage) then
//        failwithf "the specified languages array does not contain the default language '%s'" defaultLanguage

//    let js = """
//var _this = (window | this | self);
//"""
    
    